# третій студент: Шкурат Дар'я Андріївна
# Програма доповнює існуючий текстовий файл відповіддю на питання,
# яке поставив другий студент.
# Усі файлові операції виконуються з обробкою виключень.

# Ім'я файлу спільної роботи
FILENAME = "spilna_robota_python.txt"

def main():
    block = (
        "Третій студент: Шкурат Дар'я\n"
        "Відповідь на питання другого студента:\n"
        "Спискові включення (list comprehensions) у Python — це компактний спосіб створення списків\n"
        "на основі інших послідовностей (list, range, tuple, generator тощо). Вони дозволяють\n"
        "записати цикл for та (за потреби) умовний оператор if в один вираз.\n\n"
        "Приклад звичайного циклу:\n"
        "    squares = []\n"
        "    for x in range(1, 6):\n"
        "        squares.append(x * x)\n\n"
        "Те саме за допомогою спискового включення:\n"
        "    squares = [x * x for x in range(1, 6)]\n\n"
        "З умовою (фільтрацією):\n"
        "    even_squares = [x * x for x in range(1, 11) if x % 2 == 0]\n\n"
        "Порівняння:\n"
        "- звичайний цикл for більш розгорнутий, але іноді читабельніший для складної логіки;\n"
        "- спискові включення коротші й наочніші для простих перетворень та фільтрації.\n\n"
        "Питання до четвертого студента:\n"
        "Поясніть, що таке модулі та пакети в Python.\n"
        "Як створити свій власний модуль і як його імпортувати в іншу програму?\n"
        "Наведіть приклад простої структури проєкту з двома модулями.\n"
    )

    try:
        # відкриваємо файл у режимі дописування
        with open(FILENAME, "a", encoding="utf-8") as f:
            f.write("\n" + block)

        print(f"Дані третього студента успішно дописані у файл: {FILENAME}")

    except FileNotFoundError:
        print("Помилка: файл не знайдено. Спочатку його має створити перший студент.")

    except PermissionError:
        print("Помилка: немає прав на запис у файл.")

    except OSError as e:
        print(f"Сталася помилка при роботі з файлом: {e}")

if __name__ == "__main__":
    main()
